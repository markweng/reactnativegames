/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './games/saixingxing/App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => App.Enter);
